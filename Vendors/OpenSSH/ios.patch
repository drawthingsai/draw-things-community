diff --git a/clientloop.c b/clientloop.c
index 1d704a6..0686d34 100644
--- a/clientloop.c
+++ b/clientloop.c
@@ -153,6 +153,7 @@ static time_t x11_refuse_time;	/* If >0, refuse x11 opens after this time. */
 static time_t server_alive_time;	/* Time to do server_alive_check */
 static int hostkeys_update_complete;
 static int session_setup_complete;
+static int hostkeys_seen;	/* XXX use struct ssh */
 
 static void client_init_dispatch(struct ssh *ssh);
 int	session_ident = -1;
@@ -1471,6 +1472,19 @@ client_loop(struct ssh *ssh, int have_pty, int escape_char_arg,
 	int conn_in_ready, conn_out_ready;
 	sigset_t bsigset, osigset;
 
+	received_window_change_signal = 0;
+	siginfo_received = 0;
+	received_signal = 0;
+	control_persist_exit_time = 0;
+	quit_pending = 0;
+	need_rekeying = 0;
+	session_closed = 0;
+	x11_refuse_time = 0;
+	server_alive_time = 0;
+	hostkeys_update_complete = 0;
+	session_setup_complete = 0;
+	hostkeys_seen = 0;
+	TAILQ_INIT(&global_confirms);
 	debug("Entering interactive session.");
 	session_ident = ssh2_chan_id;
 
@@ -2473,7 +2486,6 @@ client_input_hostkeys(struct ssh *ssh)
 	struct sshkey *key = NULL, **tmp;
 	int r, prove_sent = 0;
 	char *fp;
-	static int hostkeys_seen = 0; /* XXX use struct ssh */
 	extern struct sockaddr_storage hostaddr; /* XXX from ssh.c */
 	struct hostkeys_update_ctx *ctx = NULL;
 	u_int want;

diff --git a/log.c b/log.c
index 2903871..3e463b7 100644
--- a/log.c
+++ b/log.c
@@ -54,15 +54,15 @@
 #include "log.h"
 #include "match.h"
 
-static LogLevel log_level = SYSLOG_LEVEL_INFO;
-static int log_on_stderr = 1;
-static int log_stderr_fd = STDERR_FILENO;
-static int log_facility = LOG_AUTH;
-static const char *argv0;
-static log_handler_fn *log_handler;
-static void *log_handler_ctx;
-static char **log_verbose;
-static size_t nlog_verbose;
+static __thread LogLevel log_level = SYSLOG_LEVEL_INFO;
+static __thread int log_on_stderr = 1;
+static __thread int log_stderr_fd = 2;
+static __thread int log_facility = LOG_AUTH;
+static __thread const char *argv0;
+static __thread log_handler_fn *log_handler;
+static __thread void *log_handler_ctx;
+static __thread char **log_verbose;
+static __thread size_t nlog_verbose;
 extern char *__progname;
 
 #define LOG_SYSLOG_VIS	(VIS_CSTYLE|VIS_NL|VIS_TAB|VIS_OCTAL)
diff --git a/includes.h b/includes.h
index 882a394..4904283 100644
--- a/includes.h
+++ b/includes.h
@@ -181,4 +181,20 @@
 
 #include "entropy.h"
 
+#if IOS_SYSTEM_OPENSSH
+#undef stderr
+#define stderr thread_stderr
+#undef stdout
+#define stdout thread_stdout
+#undef stdin
+#define stdin thread_stdin
+#undef STDIN_FILENO
+#define STDIN_FILENO fileno(thread_stdin)
+#undef STDERR_FILENO
+#define STDERR_FILENO fileno(thread_stderr)
+#undef STDOUT_FILENO
+#define STDOUT_FILENO fileno(thread_stdout)
+#define isatty ios_isatty
+#endif
+
 #endif /* INCLUDES_H */
diff --git a/misc.c b/misc.c
index 517fa7a..a3acef9 100644
--- a/misc.c
+++ b/misc.c
@@ -1304,6 +1304,13 @@ tilde_expand(const char *filename, uid_t uid, char **retp)
 		error_f("No such uid %ld", (long)uid);
 		goto out;
 	}
+#if IOS_SYSTEM_OPENSSH
+	if (user == NULL) {
+		const char *ssh_home = getenv("SSH_HOME");
+		if (ssh_home != NULL && *ssh_home != '\0')
+			pw->pw_dir = (char *)ssh_home;
+	}
+#endif
 
 	/* Make sure directory has a trailing '/' */
 	slash = (len = strlen(pw->pw_dir)) == 0 || pw->pw_dir[len - 1] != '/';
@@ -3219,6 +3226,10 @@ get_homedir(void)
 	char *cp;
 	struct passwd *pw;
 
+#if IOS_SYSTEM_OPENSSH
+	if ((cp = getenv("SSH_HOME")) != NULL && *cp != '\0')
+		return xstrdup(cp);
+#endif
 	if ((cp = getenv("HOME")) != NULL && *cp != '\0')
 		return xstrdup(cp);
 
diff --git a/readpass.c b/readpass.c
--- a/readpass.c
+++ b/readpass.c
@@ -124,0 +125,11 @@ read_passphrase(const char *prompt, int flags)
+#if IOS_SYSTEM_OPENSSH
+	int sshpass_result = ios_sshpass_read_passphrase(
+	    prompt, buf, sizeof(buf));
+	if (sshpass_result != IOS_SSHPASS_INACTIVE) {
+		if (sshpass_result != IOS_SSHPASS_PASSWORD_PROVIDED)
+			cleanup_exit(sshpass_result);
+		ret = xstrdup(buf);
+		explicit_bzero(buf, sizeof(buf));
+		return ret;
+	}
+#endif
diff --git a/openbsd-compat/readpassphrase.c b/openbsd-compat/readpassphrase.c
index ff8ff3d..4af7159 100644
--- a/openbsd-compat/readpassphrase.c
+++ b/openbsd-compat/readpassphrase.c
@@ -76,6 +76,20 @@ restart:
 	 * Read and write to /dev/tty if available.  If not, read from
 	 * stdin and write to stderr unless a tty is required.
 	 */
+#if IOS_SYSTEM_OPENSSH
+	if (flags & RPP_STDIN) {
+		input = STDIN_FILENO;
+	} else if ((input = ios_opentty()) == -1) {
+		if (flags & RPP_REQUIRE_TTY) {
+			errno = ENOTTY;
+			return(NULL);
+		}
+		input = STDIN_FILENO;
+	}
+	output = STDERR_FILENO;
+	fflush(thread_stdout);
+	fflush(thread_stderr);
+#else
 	if ((flags & RPP_STDIN) ||
 	    (input = output = open(_PATH_TTY, O_RDWR)) == -1) {
 		if (flags & RPP_REQUIRE_TTY) {
@@ -85,6 +99,7 @@ restart:
 		input = STDIN_FILENO;
 		output = STDERR_FILENO;
 	}
+#endif
 
 	/*
 	 * Turn off echo if possible.
@@ -166,8 +181,13 @@ restart:
 	(void)sigaction(SIGTSTP, &savetstp, NULL);
 	(void)sigaction(SIGTTIN, &savettin, NULL);
 	(void)sigaction(SIGTTOU, &savettou, NULL);
-	if (input != STDIN_FILENO)
+	if (input != STDIN_FILENO) {
+#if IOS_SYSTEM_OPENSSH
+		ios_closetty();
+#else
 		(void)close(input);
+#endif
+	}
 
 	/*
 	 * If we were interrupted by a signal, resend it to ourselves
diff --git a/scp.c b/scp.c
index 28ac7a2..0f669a8 100644
--- a/scp.c
+++ b/scp.c
@@ -290,6 +290,53 @@ do_cmd(char *program, char *host, char *remuser, int port, int subsystem,
 
 	/* Fork a child to execute the command on the remote host using ssh. */
 	*pid = fork();
+#if IOS_SYSTEM_OPENSSH
+	if (*pid == -1)
+		fatal("fork: %s", strerror(errno));
+#ifdef USE_PIPES
+	if (dup2(pin[0], STDIN_FILENO) == -1 ||
+	    dup2(pout[1], STDOUT_FILENO) == -1) {
+		error("dup2: %s", strerror(errno));
+		_exit(1);
+	}
+#else
+	if (dup2(sv[0], STDIN_FILENO) == -1 ||
+	    dup2(sv[0], STDOUT_FILENO) == -1) {
+		error("dup2: %s", strerror(errno));
+		_exit(1);
+	}
+#endif
+	replacearg(&args, 0, "%s", program);
+	if (port != -1) {
+		addargs(&args, "-p");
+		addargs(&args, "%d", port);
+	}
+	if (remuser != NULL) {
+		addargs(&args, "-l");
+		addargs(&args, "%s", remuser);
+	}
+	if (subsystem)
+		addargs(&args, "-s");
+	addargs(&args, "--");
+	addargs(&args, "%s", host);
+	addargs(&args, "%s", cmd);
+	(void)execvp(program, args.list);
+
+#ifdef USE_PIPES
+	close(pin[0]);
+	close(pout[1]);
+	*fdout = pin[1];
+	*fdin = pout[0];
+#else
+	close(sv[0]);
+	*fdin = sv[1];
+	*fdout = sv[1];
+#endif
+	ssh_signal(SIGTERM, killchild);
+	ssh_signal(SIGINT, killchild);
+	ssh_signal(SIGHUP, killchild);
+	return 0;
+#else
 	switch (*pid) {
 	case -1:
 		fatal("fork: %s", strerror(errno));
@@ -349,6 +396,7 @@ do_cmd(char *program, char *host, char *remuser, int port, int subsystem,
 		ssh_signal(SIGHUP, killchild);
 		return 0;
 	}
+#endif
 }
 
 /*
@@ -374,6 +422,29 @@ do_cmd2(char *host, char *remuser, int port, char *cmd,
 
 	/* Fork a child to execute the command on the remote host using ssh. */
 	pid = fork();
+#if IOS_SYSTEM_OPENSSH
+	if (pid == -1)
+		fatal("fork: %s", strerror(errno));
+	if (dup2(fdin, 0) == -1)
+		perror("dup2");
+	if (dup2(fdout, 1) == -1)
+		perror("dup2");
+
+	replacearg(&args, 0, "%s", ssh_program);
+	if (port != -1) {
+		addargs(&args, "-p");
+		addargs(&args, "%d", port);
+	}
+	if (remuser != NULL) {
+		addargs(&args, "-l");
+		addargs(&args, "%s", remuser);
+	}
+	addargs(&args, "-oBatchMode=yes");
+	addargs(&args, "--");
+	addargs(&args, "%s", host);
+	addargs(&args, "%s", cmd);
+	(void)execvp(ssh_program, args.list);
+#else
 	if (pid == 0) {
 		if (dup2(fdin, 0) == -1)
 			perror("dup2");
@@ -400,6 +471,7 @@ do_cmd2(char *host, char *remuser, int port, char *cmd,
 	} else if (pid == -1) {
 		fatal("fork: %s", strerror(errno));
 	}
+#endif
 	while (waitpid(pid, &status, 0) == -1)
 		if (errno != EINTR)
 			fatal("do_cmd2: waitpid: %s", strerror(errno));
@@ -454,11 +526,29 @@ main(int argc, char **argv)
 	char **newargv, *argv0;
 	const char *errstr;
 	extern char *optarg;
-	extern int optind;
+	extern int optind, optreset;
 	enum scp_mode_e mode = MODE_SFTP;
 	char *sftp_direct = NULL;
 	long long llv;
 
+	optind = 1;
+	optreset = 1;
+	limit_kbps = 0;
+	memset(&bwlimit, '\0', sizeof(bwlimit));
+	curfile = NULL;
+	verbose_mode = 0;
+	log_level = SYSLOG_LEVEL_INFO;
+	showprogress = 1;
+	throughlocal = 1;
+	sshport = -1;
+	ssh_program = _PATH_SSH_PROGRAM;
+	do_cmd_pid = do_cmd_pid2 = -1;
+	sftp_copy_buflen = sftp_nrequests = 0;
+	interrupted = 0;
+	errs = 0;
+	remin = remout = remin2 = remout2 = -1;
+	Tflag = pflag = iamremote = iamrecursive = targetshouldbedirectory = 0;
+
 	/* Ensure that fds 0, 1 and 2 are open or directed to /dev/null */
 	sanitise_stdfd();
 
diff --git a/sftp.c b/sftp.c
index 31d8fa4..9ecec7c 100644
--- a/sftp.c
+++ b/sftp.c
@@ -2408,6 +2408,19 @@ connect_to_server(char *path, char **args, int *in, int *out)
 
 	if ((sshpid = fork()) == -1)
 		fatal("fork: %s", strerror(errno));
+#if IOS_SYSTEM_OPENSSH
+	if ((dup2(c_in, STDIN_FILENO) == -1) ||
+	    (dup2(c_out, STDOUT_FILENO) == -1)) {
+		fprintf(stderr, "dup2: %s\n", strerror(errno));
+		_exit(1);
+	}
+	ssh_signal(SIGINT, SIG_IGN);
+	ssh_signal(SIGTERM, SIG_DFL);
+	(void)execvp(path, args);
+	close(c_in);
+	if (c_out != c_in)
+		close(c_out);
+#else
 	else if (sshpid == 0) {
 		if ((dup2(c_in, STDIN_FILENO) == -1) ||
 		    (dup2(c_out, STDOUT_FILENO) == -1)) {
@@ -2432,6 +2445,7 @@ connect_to_server(char *path, char **args, int *in, int *out)
 		fprintf(stderr, "exec: %s: %s\n", path, strerror(errno));
 		_exit(1);
 	}
+#endif
 
 	ssh_signal(SIGTERM, killchild);
 	ssh_signal(SIGINT, killchild);
@@ -2470,13 +2484,25 @@ main(int argc, char **argv)
 	const char *errstr;
 	LogLevel ll = SYSLOG_LEVEL_INFO;
 	arglist args;
-	extern int optind;
+	extern int optind, optreset;
 	extern char *optarg;
 	struct sftp_conn *conn;
 	size_t copy_buffer_len = 0;
 	size_t num_requests = 0;
 	long long llv, limit_kbps = 0;
 
+	optind = 1;
+	optreset = 1;
+	infile = stdin;
+	batchmode = 0;
+	sshpid = -1;
+	quiet = 0;
+	showprogress = 1;
+	global_rflag = global_aflag = global_pflag = global_fflag = 0;
+	interrupted = 0;
+	sort_flag = 0;
+	sort_glob = NULL;
+
 	/* Ensure that fds 0, 1 and 2 are open or directed to /dev/null */
 	sanitise_stdfd();
 	msetlocale();
diff --git a/ssh.c b/ssh.c
index 7c644c2..f1c7896 100644
--- a/ssh.c
+++ b/ssh.c
@@ -636,6 +636,19 @@ main(int ac, char **av)
 	struct utsname utsname;
 	struct ssh_conn_info *cinfo = NULL;
 
+	optind = 1;
+	optreset = 1;
+	debug_flag = 0;
+	tty_flag = 0;
+	need_controlpersist_detach = 0;
+	forward_confirms_pending = -1;
+	config = NULL;
+	host = NULL;
+	forward_agent_sock_path = NULL;
+	command = NULL;
+	muxserver_sock = -1;
+	muxclient_command = 0;
+
 	/* Ensure that fds 0, 1 and 2 are open or directed to /dev/null */
 	sanitise_stdfd();
 
@@ -643,7 +656,9 @@ main(int ac, char **av)
 	 * Discard other fds that are hanging around. These can cause problem
 	 * with backgrounded ssh processes started by ControlPersist.
 	 */
+#if !IOS_SYSTEM_OPENSSH
 	closefrom(STDERR_FILENO + 1);
+#endif
 
 	__progname = ssh_get_progname(av[0]);
 
@@ -666,6 +681,10 @@ main(int ac, char **av)
 		logit("No user exists for uid %lu", (u_long)getuid());
 		exit(255);
 	}
+#if IOS_SYSTEM_OPENSSH
+	if ((p = getenv("SSH_HOME")) != NULL && *p != '\0')
+		pw->pw_dir = p;
+#endif
 	/* Take a copy of the returned structure. */
 	pw = pwcopy(pw);
 
@@ -2195,9 +2214,15 @@ ssh_session2_setup(struct ssh *ssh, int id, int success, void *arg)
 	if ((term = lookup_env_in_list("TERM", options.setenv,
 	    options.num_setenv)) == NULL || *term == '\0')
 		term = getenv("TERM");
+#if IOS_SYSTEM_OPENSSH
+	client_session2_setup(ssh, id, tty_flag,
+	    options.session_type == SESSION_TYPE_SUBSYSTEM, term,
+	    NULL, fileno(stdin), command, environmentVariables(ios_currentPid()));
+#else
 	client_session2_setup(ssh, id, tty_flag,
 	    options.session_type == SESSION_TYPE_SUBSYSTEM, term,
 	    NULL, fileno(stdin), command, environ);
+#endif
 }
 
 /* open new channel for a session */
diff --git a/sshconnect2.c b/sshconnect2.c
index 2cb60ee..963ad00 100644
--- a/sshconnect2.c
+++ b/sshconnect2.c
@@ -418,6 +418,11 @@ Authmethod authmethods[] = {
 	{NULL, NULL, NULL, NULL, NULL}
 };
 
+static Authmethod *current;
+static char *supported;
+static char *preferred;
+static int pubkey_prepared;
+
 void
 ssh_userauth2(struct ssh *ssh, const char *local_user,
     const char *server_user, char *host, Sensitive *sensitive)
@@ -425,6 +430,11 @@ ssh_userauth2(struct ssh *ssh, const char *local_user,
 	Authctxt authctxt;
 	int r;
 
+	current = NULL;
+	free(supported);
+	supported = NULL;
+	preferred = NULL;
+	pubkey_prepared = 0;
 	if (options.preferred_authentications == NULL)
 		options.preferred_authentications = authmethods_get();
 
@@ -1869,11 +1879,10 @@ userauth_pubkey(struct ssh *ssh)
 	Identity *id;
 	int sent = 0;
 	char *ident;
-	static int prepared;
 
-	if (!prepared) {
+	if (!pubkey_prepared) {
 		pubkey_prepare(ssh, authctxt);
-		prepared = 1;
+		pubkey_prepared = 1;
 	}
 
 	while ((id = TAILQ_FIRST(&authctxt->keys))) {
@@ -2307,11 +2316,6 @@ authmethod_lookup(const char *name)
 	return NULL;
 }
 
-/* XXX internal state */
-static Authmethod *current = NULL;
-static char *supported = NULL;
-static char *preferred = NULL;
-
 /*
  * Given the authentication method list sent by the server, return the
  * next method we should try.  If the server initially sends a nil list,
diff --git a/sshconnect.c b/sshconnect.c
index 91e69b7..17aeff1 100644
--- a/sshconnect.c
+++ b/sshconnect.c
@@ -578,6 +578,12 @@ ssh_connect(struct ssh *ssh, const char *host, const char *host_arg,
 {
 	int in, out;
 
+#if IOS_SYSTEM_OPENSSH
+	sshkey_free(previous_host_key);
+	previous_host_key = NULL;
+	matching_host_key_dns = 0;
+	proxy_command_pid = 0;
+#endif
 	if (options.proxy_command == NULL) {
 		return ssh_connect_direct(ssh, host, addrs, hostaddr, port,
 		    connection_attempts, timeout_ms, want_keepalive);
