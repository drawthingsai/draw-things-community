--- a/_gen/bin/oils_for_unix.mycpp.cc
+++ b/_gen/bin/oils_for_unix.mycpp.cc
@@ -1,6 +1,9 @@
 // This file is GENERATED by mycpp, from Python source code
 
 #include "bin/oils_for_unix_preamble.h"
+#ifdef OSH_IOS
+#include "Vendors/OSH/osh_ios.h"
+#endif
 
 GLOBAL_STR(S_Aoo, "");
 GLOBAL_STR(S_Bkk, "\u0000");
@@ -11647,7 +11650,204 @@
 }
 
 }  // define namespace oils_for_unix
+
+#ifdef OSH_IOS
+
+namespace {
+
+thread_local bool gOshIosRuntimeInitialized = false;
+thread_local const osh_ios_config* gOshIosCurrentConfig = nullptr;
+thread_local bool gOshIosUnsupported = false;
+thread_local bool gOshIosMakeRedirectsPermanent = false;
+thread_local int gOshIosPermanentVirtualStdinCount = 0;
+thread_local std::vector<FILE*> gOshIosPermanentFiles;
+
+void OshIosInitRuntime() {
+  if (gOshIosRuntimeInitialized) {
+    return;
+  }
+  mylib::InitCppOnly();
+  mylib::InitOshIosWriters();
+  gOshIosRuntimeInitialized = true;
+}
+
+BigStr* OshIosReadAll(FILE* f) {
+  if (f == nullptr) {
+    return kEmptyString;
+  }
+  auto* out = Alloc<mylib::BufWriter>();
+  StackRoot _root0(&out);
+  char buf[4096];
+  while (true) {
+    size_t n = fread(buf, 1, sizeof(buf), f);
+    if (n != 0) {
+      out->write(StrFromC(buf, n));
+    }
+    if (n != sizeof(buf)) {
+      break;
+    }
+  }
+  return out->getvalue();
+}
+
+}  // namespace
+
+namespace osh_ios {
+
+const osh_ios_config* CurrentConfig() {
+  return gOshIosCurrentConfig;
+}
+
+bool IsCancelled() {
+  return gOshIosCurrentConfig != nullptr &&
+         gOshIosCurrentConfig->is_cancelled != nullptr &&
+         gOshIosCurrentConfig->is_cancelled(gOshIosCurrentConfig->context) != 0;
+}
 
+void MarkUnsupported() {
+  gOshIosUnsupported = true;
+}
+
+bool WasUnsupported() {
+  return gOshIosUnsupported;
+}
+
+static void CopyFileToWriter(FILE* file, mylib::Writer* writer) {
+  rewind(file);
+  char bytes[4096];
+  while (true) {
+    size_t count = fread(bytes, 1, sizeof(bytes), file);
+    if (count != 0) {
+      writer->write(StrFromC(bytes, count));
+    }
+    if (count != sizeof(bytes)) {
+      break;
+    }
+  }
+}
+
+int RunExternal(List<BigStr*>* argv_list,
+                Dict<BigStr*, BigStr*>* env) {
+  StackRoot _root0(&argv_list);
+  StackRoot _root1(&env);
+  if (IsCancelled()) {
+    return OSH_IOS_STATUS_CANCELLED;
+  }
+  const osh_ios_config* config = CurrentConfig();
+  if (config == nullptr || config->run_external == nullptr) {
+    MarkUnsupported();
+    mylib::Stderr()->write(
+        StrFromC("osh: unsupported in Local Code: external commands\n"));
+    return OSH_IOS_STATUS_UNSUPPORTED;
+  }
+
+  BigStr* virtual_stdin = pyos::TakeVirtualStdin();
+  StackRoot _root2(&virtual_stdin);
+  FILE* input = tmpfile();
+  FILE* output = tmpfile();
+  FILE* error = tmpfile();
+  if (input == nullptr || output == nullptr || error == nullptr) {
+    if (input != nullptr) fclose(input);
+    if (output != nullptr) fclose(output);
+    if (error != nullptr) fclose(error);
+    mylib::Stderr()->write(StrFormat(
+        "osh: unable to allocate command streams: %s\n",
+        StrFromC(strerror(errno))));
+    return 1;
+  }
+  if (virtual_stdin != nullptr) {
+    fwrite(virtual_stdin->data(), 1, len(virtual_stdin), input);
+  }
+  rewind(input);
+
+  std::vector<const char*> argv;
+  for (int i = 0; i < len(argv_list); ++i) {
+    argv.push_back(argv_list->at(i)->data());
+  }
+  std::vector<std::string> env_storage;
+  std::vector<const char*> environment;
+  env_storage.reserve(len(env));
+  environment.reserve(len(env));
+  for (DictIter<BigStr*, BigStr*> it(env); !it.Done(); it.Next()) {
+    env_storage.push_back(std::string(it.Key()->data(), len(it.Key())) + "=" +
+                          std::string(it.Value()->data(), len(it.Value())));
+  }
+  for (const auto& item : env_storage) {
+    environment.push_back(item.c_str());
+  }
+
+  int status = config->run_external(
+      static_cast<int>(argv.size()), argv.data(),
+      static_cast<int>(environment.size()), environment.data(), input, output,
+      error, config->context);
+  fflush(output);
+  fflush(error);
+  CopyFileToWriter(output, mylib::OshIosStdoutTarget());
+  CopyFileToWriter(error, mylib::OshIosStderrTarget());
+  fclose(input);
+  fclose(output);
+  fclose(error);
+  return status;
+}
+
+}  // namespace osh_ios
+
+extern "C" int osh_ios_run(const char* command, FILE* input, FILE* output,
+                             FILE* error, const osh_ios_config* config) {
+  if (command == nullptr || output == nullptr || error == nullptr ||
+      config == nullptr || config->run_external == nullptr) {
+    errno = EINVAL;
+    return 2;
+  }
+
+  OshIosInitRuntime();
+  gOshIosCurrentConfig = config;
+  gOshIosUnsupported = false;
+  gOshIosMakeRedirectsPermanent = false;
+  gOshIosPermanentVirtualStdinCount = 0;
+  gOshIosPermanentFiles.clear();
+
+  auto* input_contents = OshIosReadAll(input);
+  auto* output_file = Alloc<mylib::CFile>(output);
+  auto* error_file = Alloc<mylib::CFile>(error);
+  StackRoot _root0(&input_contents);
+  StackRoot _root1(&output_file);
+  StackRoot _root2(&error_file);
+
+  mylib::Writer* saved_stdout = mylib::SetOshIosStdoutTarget(
+      reinterpret_cast<mylib::Writer*>(output_file));
+  mylib::Writer* saved_stderr = mylib::SetOshIosStderrTarget(
+      reinterpret_cast<mylib::Writer*>(error_file));
+
+  auto* args = Alloc<List<BigStr*>>();
+  StackRoot _root3(&args);
+  args->append(StrFromC("osh"));
+  args->append(StrFromC("-c"));
+  args->append(StrFromC(command));
+
+  pyos::PushVirtualStdin(input_contents);
+  int status = oils_for_unix::main(args);
+  if (gOshIosUnsupported) {
+    status = OSH_IOS_STATUS_UNSUPPORTED;
+  }
+  mylib::Stdout()->flush();
+  mylib::Stderr()->flush();
+  for (int i = 0; i < gOshIosPermanentVirtualStdinCount; ++i) {
+    pyos::PopVirtualStdin();
+  }
+  pyos::PopVirtualStdin();
+  mylib::SetOshIosStdoutTarget(saved_stdout);
+  mylib::SetOshIosStderrTarget(saved_stderr);
+  for (FILE* file : gOshIosPermanentFiles) {
+    fclose(file);
+  }
+  gOshIosPermanentFiles.clear();
+  gOshIosCurrentConfig = nullptr;
+  return status;
+}
+
+#endif  // OSH_IOS
+
 namespace assign_osh {  // define
 
 using option_asdl::builtin_i;
@@ -18497,25 +18696,36 @@
   args::Reader* arg_r = nullptr;
   Dict<BigStr*, BigStr*>* environ = nullptr;
   int i;
+#ifndef OSH_IOS
   BigStr* cmd = nullptr;
   BigStr* argv0_path = nullptr;
+#endif
   cmd_value::Argv* c2 = nullptr;
   Tuple2<args::_Attributes*, args::Reader*> tup5 = flag_util::ParseCmdVal(S_Evy, cmd_val);
   arg_r = tup5.at1();
   if (arg_r->AtEnd()) {
+#ifdef OSH_IOS
+    gOshIosMakeRedirectsPermanent = true;
+#else
     this->fd_state->MakePermanent();
+#endif
     return 0;
   }
   environ = this->mem->GetEnv();
   i = arg_r->i;
+  c2 = Alloc<cmd_value::Argv>(cmd_val->argv->slice(i), cmd_val->arg_locs->slice(i), cmd_val->is_last_cmd, cmd_val->self_obj, nullptr);
+#ifdef OSH_IOS
+  int status = osh_ios::RunExternal(c2->argv, environ);
+  throw Alloc<util::HardExit>(status);
+#else
   cmd = cmd_val->argv->at(i);
   argv0_path = this->search_path->CachedLookup(cmd);
   if (argv0_path == nullptr) {
     e_die_status(127, StrFormat("exec: %r not found", cmd), cmd_val->arg_locs->at(1));
   }
-  c2 = Alloc<cmd_value::Argv>(cmd_val->argv->slice(i), cmd_val->arg_locs->slice(i), cmd_val->is_last_cmd, cmd_val->self_obj, nullptr);
   this->ext_prog->Exec(argv0_path, c2, environ);
   assert(0);  // AssertionError
+#endif
 }
 
 Wait::Wait(process::Waiter* waiter, process::JobList* job_list, state::Mem* mem, dev::Tracer* tracer, ui::ErrorFormatter* errfmt) {
@@ -24630,8 +24840,311 @@
 
 void PureExecutor::PopProcessSub(runtime_asdl::StatusArray* compound_st) {
   ;  // pass
+}
+
+#ifdef OSH_IOS
+
+static value_asdl::value_t* OshIosCloneValue(value_asdl::value_t* val) {
+  if (val == nullptr) {
+    return nullptr;
+  }
+  switch (val->tag()) {
+    case value_e::InternalStringArray: {
+      auto* old = static_cast<value::InternalStringArray*>(val);
+      auto* items = Alloc<List<BigStr*>>();
+      StackRoot _root0(&items);
+      for (ListIter<BigStr*> it(old->strs); !it.Done(); it.Next()) {
+        items->append(it.Value());
+      }
+      return Alloc<value::InternalStringArray>(items);
+    }
+    case value_e::BashArray:
+      return bash_impl::BashArray_Copy(static_cast<value::BashArray*>(val));
+    case value_e::BashAssoc:
+      return bash_impl::BashAssoc_Copy(static_cast<value::BashAssoc*>(val));
+    default:
+      return val;
+  }
 }
+
+class OshIosShellState {
+ public:
+  OshIosShellState(state::Mem* mem, state::MutableOpts* mutable_opts,
+                 state::Procs* procs, trap_osh::TrapState* trap_state,
+                 bool inherit_errtrace)
+      : mem_(mem),
+        mutable_opts_(mutable_opts),
+        procs_(procs),
+        trap_state_(trap_state) {
+    saved_var_stack_ = mem_->var_stack;
+    saved_last_status_ = mem_->last_status;
+    saved_pipe_status_ = mem_->pipe_status;
+    saved_process_sub_status_ = mem_->process_sub_status;
+    saved_try_status_ = mem_->try_status;
+    saved_try_error_ = mem_->try_error;
+    saved_regex_match_ = mem_->regex_match;
+    saved_ctx_stack_ = mem_->ctx_stack;
+    saved_last_arg_ = mem_->last_arg;
+    saved_token_for_line_ = mem_->token_for_line;
+    saved_loc_for_expr_ = mem_->loc_for_expr;
+    saved_opt0_array_ = mutable_opts_->opt0_array;
+    saved_opt_stacks_ = mutable_opts_->opt_stacks;
+    saved_errexit_disabled_tok_ = mutable_opts_->errexit_disabled_tok;
+    saved_sh_funcs_ = procs_->sh_funcs;
+    saved_hooks_ = trap_state_->hooks;
+    saved_traps_ = trap_state_->traps;
+    has_saved_cwd_ = getcwd(saved_cwd_, sizeof(saved_cwd_)) != nullptr;
 
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_var_stack_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_last_status_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_pipe_status_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_process_sub_status_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_try_status_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_try_error_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_regex_match_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_ctx_stack_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_last_arg_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_token_for_line_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_loc_for_expr_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_opt0_array_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_opt_stacks_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_errexit_disabled_tok_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_sh_funcs_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_hooks_));
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_traps_));
+
+    auto* child_var_stack =
+        Alloc<List<Dict<BigStr*, runtime_asdl::Cell*>*>>();
+    StackRoot _root0(&child_var_stack);
+    for (ListIter<Dict<BigStr*, runtime_asdl::Cell*>*> it(saved_var_stack_);
+         !it.Done(); it.Next()) {
+      auto* child_frame = Alloc<Dict<BigStr*, runtime_asdl::Cell*>>();
+      StackRoot _root1(&child_frame);
+      for (DictIter<BigStr*, runtime_asdl::Cell*> cell_it(it.Value());
+           !cell_it.Done(); cell_it.Next()) {
+        runtime_asdl::Cell* cell = cell_it.Value();
+        value_asdl::value_t* cloned_value = OshIosCloneValue(cell->val);
+        StackRoot _root2(&cloned_value);
+        child_frame->set(
+            cell_it.Key(),
+            Alloc<runtime_asdl::Cell>(cell->exported, cell->readonly,
+                                      cell->nameref, cloned_value));
+      }
+      child_var_stack->append(child_frame);
+    }
+    mem_->var_stack = child_var_stack;
+    mem_->last_status = NewList<int>(
+        std::initializer_list<int>{saved_last_status_->at(-1)});
+    mem_->pipe_status = NewList<List<int>*>(
+        std::initializer_list<List<int>*>{Alloc<List<int>>()});
+    mem_->process_sub_status = NewList<List<int>*>(
+        std::initializer_list<List<int>*>{Alloc<List<int>>()});
+    mem_->try_status = NewList<int>(
+        std::initializer_list<int>{saved_try_status_->at(-1)});
+    mem_->try_error = NewList<value::Dict*>(
+        std::initializer_list<value::Dict*>{saved_try_error_->at(-1)});
+    mem_->regex_match = NewList<value_asdl::regex_match_t*>(
+        std::initializer_list<value_asdl::regex_match_t*>{
+            saved_regex_match_->at(-1)});
+    mem_->ctx_stack = Alloc<List<Dict<BigStr*, value_asdl::value_t*>*>>();
+
+    auto* child_opt0 = Alloc<List<bool>>();
+    StackRoot _root3(&child_opt0);
+    for (int i = 0; i < len(saved_opt0_array_); ++i) {
+      child_opt0->append(saved_opt0_array_->at(i));
+    }
+    auto* child_opt_stacks = Alloc<List<List<bool>*>>();
+    StackRoot _root4(&child_opt_stacks);
+    for (int i = 0; i < len(saved_opt_stacks_); ++i) {
+      List<bool>* saved_stack = saved_opt_stacks_->at(i);
+      if (saved_stack == nullptr) {
+        child_opt_stacks->append(nullptr);
+      } else {
+        auto* child_stack = Alloc<List<bool>>();
+        for (int j = 0; j < len(saved_stack); ++j) {
+          child_stack->append(saved_stack->at(j));
+        }
+        child_opt_stacks->append(child_stack);
+      }
+    }
+    mutable_opts_->opt0_array = child_opt0;
+    mutable_opts_->opt_stacks = child_opt_stacks;
+    mutable_opts_->errexit_disabled_tok = Alloc<List<syntax_asdl::Token*>>();
+
+    auto* child_sh_funcs = Alloc<Dict<BigStr*, value_asdl::value_t*>>();
+    StackRoot _root5(&child_sh_funcs);
+    for (DictIter<BigStr*, value_asdl::value_t*> it(saved_sh_funcs_);
+         !it.Done(); it.Next()) {
+      child_sh_funcs->set(it.Key(), it.Value());
+    }
+    procs_->sh_funcs = child_sh_funcs;
+
+    auto* child_hooks = Alloc<Dict<BigStr*, syntax_asdl::command_t*>>();
+    StackRoot _root6(&child_hooks);
+    syntax_asdl::command_t* err_hook = saved_hooks_->get(S_zDr, nullptr);
+    StackRoot _root7(&err_hook);
+    if (inherit_errtrace && err_hook != nullptr) {
+      child_hooks->set(S_zDr, err_hook);
+    }
+    trap_state_->hooks = child_hooks;
+    trap_state_->traps = Alloc<Dict<int, syntax_asdl::command_t*>>();
+  }
+
+  ~OshIosShellState() {
+    if (has_saved_cwd_) {
+      chdir(saved_cwd_);
+    }
+    mem_->var_stack = saved_var_stack_;
+    mem_->last_status = saved_last_status_;
+    mem_->pipe_status = saved_pipe_status_;
+    mem_->process_sub_status = saved_process_sub_status_;
+    mem_->try_status = saved_try_status_;
+    mem_->try_error = saved_try_error_;
+    mem_->regex_match = saved_regex_match_;
+    mem_->ctx_stack = saved_ctx_stack_;
+    mem_->last_arg = saved_last_arg_;
+    mem_->token_for_line = saved_token_for_line_;
+    mem_->loc_for_expr = saved_loc_for_expr_;
+    mutable_opts_->opt0_array = saved_opt0_array_;
+    mutable_opts_->opt_stacks = saved_opt_stacks_;
+    mutable_opts_->errexit_disabled_tok = saved_errexit_disabled_tok_;
+    procs_->sh_funcs = saved_sh_funcs_;
+    for (DictIter<int, syntax_asdl::command_t*> it(trap_state_->traps);
+         !it.Done(); it.Next()) {
+      int sig_num = it.Key();
+      if (!dict_contains(saved_traps_, sig_num)) {
+        if (sig_num == SIGINT) {
+          trap_state_->signal_safe->SetSigIntTrapped(false);
+        } else if (sig_num == SIGWINCH) {
+          trap_state_->signal_safe->SetSigWinchCode(iolib::UNTRAPPED_SIGWINCH);
+        } else {
+          iolib::sigaction(sig_num, SIG_DFL);
+        }
+      }
+    }
+    for (DictIter<int, syntax_asdl::command_t*> it(saved_traps_);
+         !it.Done(); it.Next()) {
+      int sig_num = it.Key();
+      if (sig_num == SIGINT) {
+        trap_state_->signal_safe->SetSigIntTrapped(true);
+      } else if (sig_num == SIGWINCH) {
+        trap_state_->signal_safe->SetSigWinchCode(SIGWINCH);
+      } else {
+        iolib::RegisterSignalInterest(sig_num);
+      }
+    }
+    trap_state_->hooks = saved_hooks_;
+    trap_state_->traps = saved_traps_;
+    for (int i = 0; i < 17; ++i) {
+      gHeap.PopRoot();
+    }
+  }
+
+ private:
+  state::Mem* mem_;
+  state::MutableOpts* mutable_opts_;
+  state::Procs* procs_;
+  trap_osh::TrapState* trap_state_;
+  List<Dict<BigStr*, runtime_asdl::Cell*>*>* saved_var_stack_;
+  List<int>* saved_last_status_;
+  List<List<int>*>* saved_pipe_status_;
+  List<List<int>*>* saved_process_sub_status_;
+  List<int>* saved_try_status_;
+  List<value::Dict*>* saved_try_error_;
+  List<value_asdl::regex_match_t*>* saved_regex_match_;
+  List<Dict<BigStr*, value_asdl::value_t*>*>* saved_ctx_stack_;
+  BigStr* saved_last_arg_;
+  syntax_asdl::Token* saved_token_for_line_;
+  syntax_asdl::loc_t* saved_loc_for_expr_;
+  List<bool>* saved_opt0_array_;
+  List<List<bool>*>* saved_opt_stacks_;
+  List<syntax_asdl::Token*>* saved_errexit_disabled_tok_;
+  Dict<BigStr*, value_asdl::value_t*>* saved_sh_funcs_;
+  Dict<BigStr*, syntax_asdl::command_t*>* saved_hooks_;
+  Dict<int, syntax_asdl::command_t*>* saved_traps_;
+  bool has_saved_cwd_;
+  char saved_cwd_[4096];
+};
+
+class OshIosIoScope {
+ public:
+  OshIosIoScope(BigStr* stdin_contents, mylib::Writer* stdout_writer)
+      : has_stdin_(stdin_contents != nullptr) {
+    saved_stdout_ = mylib::SetOshIosStdoutTarget(stdout_writer);
+    gHeap.PushRoot(reinterpret_cast<RawObject**>(&saved_stdout_));
+    if (has_stdin_) {
+      pyos::PushVirtualStdin(stdin_contents);
+    }
+  }
+
+  ~OshIosIoScope() {
+    if (has_stdin_) {
+      pyos::PopVirtualStdin();
+    }
+    mylib::SetOshIosStdoutTarget(saved_stdout_);
+    gHeap.PopRoot();
+  }
+
+ private:
+  mylib::Writer* saved_stdout_;
+  bool has_stdin_;
+};
+
+static void OshIosUnsupported(const char* feature) {
+  osh_ios::MarkUnsupported();
+  mylib::Stderr()->write(
+      StrFormat("osh: unsupported in Local Code: %s\n", StrFromC(feature)));
+}
+
+static int OshIosExecuteIsolated(ShellExecutor* shell_ex,
+                               syntax_asdl::command_t* node,
+                               BigStr* stdin_contents,
+                               mylib::Writer* stdout_writer,
+                               bool inherit_errexit,
+                               bool inherit_errtrace) {
+  StackRoot _root0(&shell_ex);
+  StackRoot _root1(&node);
+  StackRoot _root2(&stdin_contents);
+  StackRoot _root3(&stdout_writer);
+  OshIosShellState child_state(shell_ex->mem, shell_ex->mutable_opts,
+                             shell_ex->procs, shell_ex->trap_state,
+                             inherit_errtrace);
+  OshIosIoScope child_io(stdin_contents, stdout_writer);
+  if (!inherit_errexit) {
+    shell_ex->mutable_opts->DisableErrExit();
+  }
+  int status;
+  try {
+    shell_ex->cmd_ev->ExecuteAndCatch(node, cmd_eval::OptimizeSubshells);
+    status = shell_ex->cmd_ev->LastStatus();
+  } catch (util::HardExit* e) {
+    status = e->status;
+  }
+  syntax_asdl::IntParamBox* mut_status =
+      Alloc<syntax_asdl::IntParamBox>(status);
+  StackRoot _root4(&mut_status);
+  shell_ex->cmd_ev->RunTrapsOnExit(mut_status);
+  return mut_status->i;
+}
+
+static int OshIosRunExternal(ShellExecutor* shell_ex,
+                           cmd_value::Argv* cmd_val) {
+  return osh_ios::RunExternal(cmd_val->argv, shell_ex->mem->GetEnv());
+}
+
+struct OshIosRedirectFrame {
+  mylib::Writer* saved_stdout;
+  mylib::Writer* saved_stderr;
+  StackRoot* stdout_root;
+  StackRoot* stderr_root;
+  int virtual_stdin_count;
+  std::vector<FILE*> opened_files;
+};
+
+thread_local std::vector<OshIosRedirectFrame*> gOshIosRedirectStack;
+
+#endif  // OSH_IOS
+
 ShellExecutor::ShellExecutor(state::Mem* mem, optview::Exec* exec_opts, state::MutableOpts* mutable_opts, state::Procs* procs, hay_ysh::HayState* hay_state, Dict<int, vm::_Builtin*>* builtins, dev::Tracer* tracer, ui::ErrorFormatter* errfmt, executor::SearchPath* search_path, process::ExternalProgram* ext_prog, process::Waiter* waiter, process::JobControl* job_control, process::JobList* job_list, process::FdState* fd_state, trap_osh::TrapState* trap_state) : ::vm::_Executor(mem, exec_opts, mutable_opts, procs, hay_state, builtins, tracer, errfmt) {
   this->search_path = search_path;
   this->ext_prog = ext_prog;
@@ -24719,10 +25232,14 @@
     builtin_id = _RewriteExternToBuiltin(cmd_val->argv);
     if (builtin_id != consts::NO_INDEX) {
       if (builtin_id == builtin_i::cat) {
+#ifdef OSH_IOS
+        return this->RunExternal(arg0, arg0_loc, cmd_val, cmd_st, run_flags);
+#else
         thunk = Alloc<process::BuiltinThunk>(this, builtin_id, cmd_val);
         p = Alloc<process::Process>(thunk, this->job_control, this->job_list, this->tracer);
         status = p->RunProcess(this->waiter, trace::Fork);
         return status;
+#endif
       }
       else {
         return this->RunBuiltin(builtin_id, cmd_val);
@@ -24733,6 +25250,18 @@
 }
 
 int ShellExecutor::RunExternal(BigStr* arg0, syntax_asdl::loc_t* arg0_loc, cmd_value::Argv* cmd_val, runtime_asdl::CommandStatus* cmd_st, int run_flags) {
+#ifdef OSH_IOS
+  this->tracer->OnExec(cmd_val->argv);
+  int status = OshIosRunExternal(this, cmd_val);
+  if (status == 127) {
+    this->errfmt->Print_(StrFormat("Command %r not found (OILS-ERR-100)", arg0),
+                         arg0_loc);
+  }
+  if (cmd_st != nullptr) {
+    cmd_st->show_code = true;
+  }
+  return status;
+#else
   Dict<BigStr*, BigStr*>* environ = nullptr;
   BigStr* argv0_path = nullptr;
   bool do_fork;
@@ -24792,9 +25321,18 @@
   this->tracer->OnExec(cmd_val->argv);
   this->ext_prog->Exec(argv0_path, cmd_val, environ);
   assert(0);  // AssertionError
+#endif
 }
 
 int ShellExecutor::RunBackgroundJob(syntax_asdl::command_t* node) {
+#ifdef OSH_IOS
+  mylib::Stderr()->write(StrFromC(
+      "osh: warning: '&' requested a background job; "
+      "running it in the current Bash job instead\n"));
+  return OshIosExecuteIsolated(
+      this, node, nullptr, mylib::OshIosStdoutTarget(), true,
+      this->exec_opts->errtrace());
+#else
   syntax_asdl::command_t* UP_node = nullptr;
   process::Pipeline* pi = nullptr;
   process::Process* p = nullptr;
@@ -24832,9 +25370,51 @@
     print_stderr(StrFormat("[%%%d] PID %d Started", job_id, this->mem->last_bg_pid));
   }
   return 0;
+#endif
 }
 
 void ShellExecutor::RunPipeline(command::Pipeline* node, runtime_asdl::CommandStatus* status_out) {
+#ifdef OSH_IOS
+  List<int>* codes = Alloc<List<int>>();
+  List<syntax_asdl::loc_t*>* pipe_locs =
+      Alloc<List<syntax_asdl::loc_t*>>();
+  BigStr* previous_output = nullptr;
+  mylib::BufWriter* stage_output = nullptr;
+  mylib::Writer* parent_output = mylib::OshIosStdoutTarget();
+  StackRoot _root0(&node);
+  StackRoot _root1(&status_out);
+  StackRoot _root2(&codes);
+  StackRoot _root3(&pipe_locs);
+  StackRoot _root4(&previous_output);
+  StackRoot _root5(&stage_output);
+  StackRoot _root6(&parent_output);
+
+  int n = len(node->children);
+  for (int i = 0; i < n; ++i) {
+    if (osh_ios::IsCancelled()) {
+      throw Alloc<util::HardExit>(OSH_IOS_STATUS_CANCELLED);
+    }
+    syntax_asdl::command_t* child = node->children->at(i);
+    bool is_last = i == n - 1;
+    stage_output = is_last ? nullptr : Alloc<mylib::BufWriter>();
+    mylib::Writer* output = is_last
+        ? parent_output
+        : static_cast<mylib::Writer*>(stage_output);
+    int status = OshIosExecuteIsolated(this, child, previous_output, output, true,
+                                     this->exec_opts->errtrace());
+    codes->append(status);
+    pipe_locs->append(Alloc<loc::Command>(child));
+    if (!is_last) {
+      if (stage_output->Length() > 16 * 1024 * 1024) {
+        OshIosUnsupported("pipeline stage output larger than 16 MiB");
+        throw Alloc<util::HardExit>(OSH_IOS_STATUS_UNSUPPORTED);
+      }
+      previous_output = stage_output->getvalue();
+    }
+  }
+  status_out->pipe_status = codes;
+  status_out->pipe_locs = pipe_locs;
+#else
   process::Pipeline* pi = nullptr;
   List<syntax_asdl::loc_t*>* pipe_locs = nullptr;
   int n;
@@ -24868,9 +25448,14 @@
     this->fg_pipeline = nullptr;
   }
   status_out->pipe_locs = pipe_locs;
+#endif
 }
 
 int ShellExecutor::RunSubshell(syntax_asdl::command_t* node) {
+#ifdef OSH_IOS
+  return OshIosExecuteIsolated(this, node, nullptr, mylib::OshIosStdoutTarget(),
+                             true, this->exec_opts->errtrace());
+#else
   process::Process* p = nullptr;
   StackRoot _root0(&node);
   StackRoot _root1(&p);
@@ -24880,9 +25465,20 @@
     p->AddStateChange(Alloc<process::SetPgid>(process::OWN_LEADER, this->tracer));
   }
   return p->RunProcess(this->waiter, trace::ForkWait);
+#endif
 }
 
 Tuple2<int, BigStr*> ShellExecutor::CaptureStdout(syntax_asdl::command_t* node) {
+#ifdef OSH_IOS
+  mylib::BufWriter* output = Alloc<mylib::BufWriter>();
+  StackRoot _root0(&node);
+  StackRoot _root1(&output);
+  int status = OshIosExecuteIsolated(
+      this, node, nullptr, output, this->exec_opts->inherit_errexit(),
+      this->exec_opts->errtrace());
+  BigStr* stdout_str = output->getvalue()->rstrip(S_nfs);
+  return Tuple2<int, BigStr*>(status, stdout_str);
+#else
   process::Process* p = nullptr;
   int r;
   int w;
@@ -24928,9 +25524,14 @@
   status = p->Wait(this->waiter);
   stdout_str = S_Aoo->join(chunks)->rstrip(S_nfs);
   return Tuple2<int, BigStr*>(status, stdout_str);
+#endif
 }
 
 Tuple3<int, BigStr*, BigStr*> ShellExecutor::Capture3(syntax_asdl::command_t* node) {
+#ifdef OSH_IOS
+  OshIosUnsupported("capture of stdout and stderr as separate values");
+  throw Alloc<util::HardExit>(OSH_IOS_STATUS_UNSUPPORTED);
+#else
   process::Process* p = nullptr;
   int stdout_fd;
   int w;
@@ -25006,6 +25607,7 @@
   stdout_str = S_Aoo->join(stdout_chunks);
   stderr_str = S_Aoo->join(stderr_chunks);
   return Tuple3<int, BigStr*, BigStr*>(status, stdout_str, stderr_str);
+#endif
 }
 
 BigStr* ShellExecutor::RunCommandSub(syntax_asdl::CommandSub* cs_part) {
@@ -25054,6 +25656,10 @@
 }
 
 BigStr* ShellExecutor::RunProcessSub(syntax_asdl::CommandSub* cs_part) {
+#ifdef OSH_IOS
+  OshIosUnsupported("process substitution (<(...) and >(...))");
+  throw Alloc<util::HardExit>(OSH_IOS_STATUS_UNSUPPORTED);
+#else
   loc::WordPart* cs_loc = nullptr;
   process::Process* p = nullptr;
   int r;
@@ -25115,9 +25721,136 @@
       assert(0);  // AssertionError
     }
   }
+#endif
 }
 
 void ShellExecutor::PushRedirects(List<runtime_asdl::RedirValue*>* redirects, List<int>* err_out) {
+#ifdef OSH_IOS
+  if (len(redirects) == 0) {
+    return;
+  }
+  auto* frame = new OshIosRedirectFrame();
+  frame->saved_stdout = mylib::OshIosStdoutTarget();
+  frame->saved_stderr = mylib::OshIosStderrTarget();
+  frame->stdout_root = new StackRoot(&frame->saved_stdout);
+  frame->stderr_root = new StackRoot(&frame->saved_stderr);
+  frame->virtual_stdin_count = 0;
+
+  for (ListIter<runtime_asdl::RedirValue*> it(redirects);
+       !it.Done(); it.Next()) {
+    runtime_asdl::RedirValue* r = it.Value();
+    if (r->loc->tag() != syntax_asdl::redir_loc_e::Fd) {
+      OshIosUnsupported("named file-descriptor redirection");
+      err_out->append(ENOTSUP);
+      break;
+    }
+    int target_fd = static_cast<syntax_asdl::redir_loc::Fd*>(r->loc)->fd;
+
+    if (r->arg->tag() == runtime_asdl::redirect_arg_e::CopyFd) {
+      int source_fd =
+          static_cast<runtime_asdl::redirect_arg::CopyFd*>(r->arg)->target_fd;
+      if (target_fd == STDERR_FILENO && source_fd == STDOUT_FILENO) {
+        mylib::SetOshIosStderrTarget(mylib::OshIosStdoutTarget());
+        continue;
+      }
+      if (target_fd == STDOUT_FILENO && source_fd == STDERR_FILENO) {
+        mylib::SetOshIosStdoutTarget(mylib::OshIosStderrTarget());
+        continue;
+      }
+      OshIosUnsupported("file-descriptor duplication other than 2>&1 or 1>&2");
+      err_out->append(ENOTSUP);
+      break;
+    }
+
+    if (r->arg->tag() == runtime_asdl::redirect_arg_e::HereDoc &&
+        target_fd == STDIN_FILENO) {
+      auto* here_doc =
+          static_cast<runtime_asdl::redirect_arg::HereDoc*>(r->arg);
+      pyos::PushVirtualStdin(here_doc->body);
+      frame->virtual_stdin_count++;
+      continue;
+    }
+
+    if (r->arg->tag() != runtime_asdl::redirect_arg_e::Path) {
+      OshIosUnsupported("move/close file-descriptor redirection");
+      err_out->append(ENOTSUP);
+      break;
+    }
+    auto* path = static_cast<runtime_asdl::redirect_arg::Path*>(r->arg);
+
+    if (target_fd == STDIN_FILENO && r->op_id == Id::Redir_Less) {
+      FILE* f = fopen(path->filename->data(), "rb");
+      if (f == nullptr) {
+        err_out->append(errno);
+        break;
+      }
+      BigStr* input_contents = nullptr;
+      {
+        auto* contents = Alloc<mylib::BufWriter>();
+        StackRoot _root0(&contents);
+        char bytes[4096];
+        while (true) {
+          size_t count = fread(bytes, 1, sizeof(bytes), f);
+          if (count != 0) {
+            contents->write(StrFromC(bytes, count));
+          }
+          if (count != sizeof(bytes)) {
+            break;
+          }
+        }
+        input_contents = contents->getvalue();
+      }
+      fclose(f);
+      pyos::PushVirtualStdin(input_contents);
+      frame->virtual_stdin_count++;
+      continue;
+    }
+
+    if (target_fd != STDOUT_FILENO && target_fd != STDERR_FILENO) {
+      OshIosUnsupported("redirection of file descriptors other than 0, 1, and 2");
+      err_out->append(ENOTSUP);
+      break;
+    }
+    const char* mode = nullptr;
+    if (r->op_id == Id::Redir_Great || r->op_id == Id::Redir_Clobber) {
+      mode = "wb";
+    } else if (r->op_id == Id::Redir_DGreat) {
+      mode = "ab";
+    } else {
+      OshIosUnsupported("this redirection operator");
+      err_out->append(ENOTSUP);
+      break;
+    }
+    FILE* f = fopen(path->filename->data(), mode);
+    if (f == nullptr) {
+      err_out->append(errno);
+      break;
+    }
+    frame->opened_files.push_back(f);
+    auto* writer = Alloc<mylib::CFile>(f);
+    if (target_fd == STDOUT_FILENO) {
+      mylib::SetOshIosStdoutTarget(reinterpret_cast<mylib::Writer*>(writer));
+    } else {
+      mylib::SetOshIosStderrTarget(reinterpret_cast<mylib::Writer*>(writer));
+    }
+  }
+
+  if (len(err_out) != 0) {
+    mylib::SetOshIosStdoutTarget(frame->saved_stdout);
+    mylib::SetOshIosStderrTarget(frame->saved_stderr);
+    for (int i = 0; i < frame->virtual_stdin_count; ++i) {
+      pyos::PopVirtualStdin();
+    }
+    for (FILE* f : frame->opened_files) {
+      fclose(f);
+    }
+    delete frame->stderr_root;
+    delete frame->stdout_root;
+    delete frame;
+    return;
+  }
+  gOshIosRedirectStack.push_back(frame);
+#else
   StackRoot _root0(&redirects);
   StackRoot _root1(&err_out);
 
@@ -25125,13 +25858,51 @@
     return ;
   }
   this->fd_state->Push(redirects, err_out);
+#endif
 }
 
 void ShellExecutor::PopRedirects(int num_redirects, List<int>* err_out) {
+#ifdef OSH_IOS
   if (num_redirects == 0) {
+    gOshIosMakeRedirectsPermanent = false;
+    return;
+  }
+  assert(!gOshIosRedirectStack.empty());
+  OshIosRedirectFrame* frame = gOshIosRedirectStack.back();
+  gOshIosRedirectStack.pop_back();
+  if (gOshIosMakeRedirectsPermanent) {
+    gOshIosPermanentVirtualStdinCount += frame->virtual_stdin_count;
+    gOshIosPermanentFiles.insert(gOshIosPermanentFiles.end(),
+                               frame->opened_files.begin(),
+                               frame->opened_files.end());
+    gOshIosMakeRedirectsPermanent = false;
+    delete frame->stderr_root;
+    delete frame->stdout_root;
+    delete frame;
+    return;
+  }
+  mylib::SetOshIosStdoutTarget(frame->saved_stdout);
+  mylib::SetOshIosStderrTarget(frame->saved_stderr);
+  for (int i = 0; i < frame->virtual_stdin_count; ++i) {
+    pyos::PopVirtualStdin();
+  }
+  for (FILE* f : frame->opened_files) {
+    if (fflush(f) != 0 && len(err_out) == 0) {
+      err_out->append(errno);
+    }
+    if (fclose(f) != 0 && len(err_out) == 0) {
+      err_out->append(errno);
+    }
+  }
+  delete frame->stderr_root;
+  delete frame->stdout_root;
+  delete frame;
+#else
+  if (num_redirects == 0) {
     return ;
   }
   this->fd_state->Pop(err_out);
+#endif
 }
 
 void ShellExecutor::PushProcessSub() {
@@ -38080,6 +38851,11 @@
 }
 
 int CommandEvaluator::_Execute(syntax_asdl::command_t* node) {
+#ifdef OSH_IOS
+  if (osh_ios::IsCancelled()) {
+    throw Alloc<util::HardExit>(OSH_IOS_STATUS_CANCELLED);
+  }
+#endif
   runtime_asdl::CommandStatus* cmd_st = nullptr;
   runtime_asdl::StatusArray* process_sub_st = nullptr;
   int status;
@@ -58274,4 +59050,3 @@
 }
 
 }  // define namespace shell
-

--- a/cpp/core.cc
+++ b/cpp/core.cc
@@ -23,12 +23,52 @@
 #include "_gen/cpp/build_stamp.h"        // gCommitHash
 #include "_gen/frontend/consts.h"        // gVersion
 #include "cpp/embedded_file.h"
+#ifdef OSH_IOS
+#include "Vendors/OSH/osh_ios.h"
+#endif
 #include "mycpp/gc_iolib.h"
 
 extern char** environ;
 
 namespace pyos {
 
+#ifdef OSH_IOS
+struct VirtualStdinFrame {
+  BigStr* contents;
+  int pos;
+  StackRoot* root;
+};
+
+thread_local std::vector<VirtualStdinFrame*> gVirtualStdin;
+
+void PushVirtualStdin(BigStr* contents) {
+  auto* frame = new VirtualStdinFrame{contents, 0, nullptr};
+  gVirtualStdin.push_back(frame);
+  frame->root = new StackRoot(&frame->contents);
+}
+
+void PopVirtualStdin() {
+  assert(!gVirtualStdin.empty());
+  delete gVirtualStdin.back()->root;
+  delete gVirtualStdin.back();
+  gVirtualStdin.pop_back();
+}
+
+BigStr* TakeVirtualStdin() {
+  if (gVirtualStdin.empty()) {
+    return nullptr;
+  }
+  VirtualStdinFrame* frame = gVirtualStdin.back();
+  int remaining = len(frame->contents) - frame->pos;
+  BigStr* result = NewStr(remaining);
+  if (remaining != 0) {
+    memcpy(result->data(), frame->contents->data() + frame->pos, remaining);
+  }
+  frame->pos = len(frame->contents);
+  return result;
+}
+#endif
+
 Tuple2<int, int> WaitPid(int waitpid_options) {
   int status;
   int result = ::waitpid(-1, &status, WUNTRACED | waitpid_options);
@@ -42,6 +82,21 @@
 }
 
 Tuple2<int, int> Read(int fd, int n, List<BigStr*>* chunks) {
+#ifdef OSH_IOS
+  if (fd == STDIN_FILENO && !gVirtualStdin.empty()) {
+    VirtualStdinFrame* frame = gVirtualStdin.back();
+    int remaining = len(frame->contents) - frame->pos;
+    int length = remaining < n ? remaining : n;
+    if (length == 0) {
+      return Tuple2<int, int>(0, 0);
+    }
+    BigStr* s = NewStr(length);
+    memcpy(s->data(), frame->contents->data() + frame->pos, length);
+    frame->pos += length;
+    chunks->append(s);
+    return Tuple2<int, int>(length, 0);
+  }
+#endif
   BigStr* s = OverAllocatedStr(n);  // Allocate enough for the result
 
   int length = ::read(fd, s->data(), n);
@@ -63,6 +118,16 @@
 }
 
 Tuple2<int, int> ReadByte(int fd) {
+#ifdef OSH_IOS
+  if (fd == STDIN_FILENO && !gVirtualStdin.empty()) {
+    VirtualStdinFrame* frame = gVirtualStdin.back();
+    if (frame->pos == len(frame->contents)) {
+      return Tuple2<int, int>(EOF_SENTINEL, 0);
+    }
+    int ch = static_cast<unsigned char>(frame->contents->data()[frame->pos++]);
+    return Tuple2<int, int>(ch, 0);
+  }
+#endif
   unsigned char buf[1];
   ssize_t n = read(fd, &buf, 1);
   if (n < 0) {  // read error
@@ -80,6 +145,20 @@
 Dict<BigStr*, BigStr*>* Environ() {
   auto d = Alloc<Dict<BigStr*, BigStr*>>();
 
+#ifdef OSH_IOS
+  const osh_ios_config* config = osh_ios::CurrentConfig();
+  if (config != nullptr && config->environment != nullptr) {
+    for (int i = 0; i < config->environment_count; ++i) {
+      const char* pair = config->environment[i];
+      const char* eq = strchr(pair, '=');
+      if (eq == nullptr) {
+        continue;
+      }
+      d->set(StrFromC(pair, eq - pair), StrFromC(eq + 1));
+    }
+    return d;
+  }
+#endif
   for (char** env = environ; *env; ++env) {
     char* pair = *env;
@@ -506,7 +585,11 @@
   return s;
 }
 
+#ifdef OSH_IOS
+static thread_local grammar::Grammar* gOilGrammar = nullptr;
+#else
 static grammar::Grammar* gOilGrammar = nullptr;
+#endif

 grammar::Grammar* LoadYshGrammar(_ResourceLoader*) {
   if (gOilGrammar != nullptr) {


--- a/cpp/core.h
+++ b/cpp/core.h
@@ -23,6 +23,12 @@
 const int NEWLINE_CH = 10;
 const int BACKSLASH_CH = 92;
 
+#ifdef OSH_IOS
+void PushVirtualStdin(BigStr* contents);
+void PopVirtualStdin();
+BigStr* TakeVirtualStdin();
+#endif
+
 Tuple2<int, int> WaitPid(int waitpid_options);
 Tuple2<int, int> Read(int fd, int n, List<BigStr*>* chunks);
 Tuple2<int, int> ReadByte(int fd);

--- a/cpp/fanos.cc
+++ b/cpp/fanos.cc
@@ -1,6 +1,7 @@
 // fanos.cc
 
 #include "cpp/fanos.h"
+#include "cpp/fanos_shared.c"
 
 #include <assert.h>
 #include <errno.h>

--- a/cpp/stdlib.cc
+++ b/cpp/stdlib.cc
@@ -17,6 +17,9 @@
 #include "mycpp/runtime.h"
 // To avoid circular dependency with e_die()
 #include "prebuilt/core/error.mycpp.h"
+#ifdef OSH_IOS
+#include "Vendors/OSH/osh_ios.h"
+#endif
 
 using error::e_die;
 
@@ -145,12 +148,32 @@
 }
 
 void kill(int pid, int sig) {
+#ifdef OSH_IOS
+  const osh_ios_config* config = osh_ios::CurrentConfig();
+  if (config != nullptr && config->send_signal != nullptr) {
+    int result = config->send_signal(pid, sig, 0, config->context);
+    if (result != 0) {
+      throw Alloc<OSError>(result);
+    }
+    return;
+  }
+#endif
   if (::kill(pid, sig) != 0) {
     throw Alloc<OSError>(errno);
   }
 }
 
 void killpg(int pgid, int sig) {
+#ifdef OSH_IOS
+  const osh_ios_config* config = osh_ios::CurrentConfig();
+  if (config != nullptr && config->send_signal != nullptr) {
+    int result = config->send_signal(pgid, sig, 1, config->context);
+    if (result != 0) {
+      throw Alloc<OSError>(result);
+    }
+    return;
+  }
+#endif
   if (::killpg(pgid, sig) != 0) {
     throw Alloc<OSError>(errno);
   }

--- a/cpp/stdlib.h
+++ b/cpp/stdlib.h
@@ -8,6 +8,9 @@
 #include <unistd.h>
 
 #include "mycpp/runtime.h"
+#ifdef OSH_IOS
+#include "Vendors/OSH/osh_ios.h"
+#endif
 
 namespace fcntl_ {
 
@@ -48,6 +51,12 @@
 }
 
 inline int getpid() {
+#ifdef OSH_IOS
+  const osh_ios_config* config = osh_ios::CurrentConfig();
+  if (config != nullptr && config->get_process_id != nullptr) {
+    return config->get_process_id(config->context);
+  }
+#endif
   return ::getpid();
 }
 

--- a/mycpp/gc_builtins.cc
+++ b/mycpp/gc_builtins.cc
@@ -5,12 +5,20 @@
 
 #include "_build/detected-cpp-config.h"
 #include "mycpp/gc_list.h"
+#ifdef OSH_IOS
+#include "mycpp/gc_mylib.h"
+#endif
 #include "mycpp/gc_str.h"
 
 // Translation of Python's print().
 void print(BigStr* s) {
+#ifdef OSH_IOS
+  mylib::Stdout()->write(s);
+  mylib::Stdout()->write(StrFromC("\n"));
+#else
   fputs(s->data_, stdout);  // print until first NUL
   fputc('\n', stdout);
+#endif
 }
 
 BigStr* str(int i) {

--- a/mycpp/gc_iolib.cc
+++ b/mycpp/gc_iolib.cc
@@ -1,6 +1,9 @@
 #include "mycpp/gc_iolib.h"
 
 #include <errno.h>
+#ifdef OSH_IOS
+#include "Vendors/OSH/osh_ios.h"
+#endif
 
 namespace iolib {
 
@@ -7,11 +10,17 @@
+#ifdef OSH_IOS
+thread_local SignalSafe* gSignalSafe = nullptr;
+#else
 SignalSafe* gSignalSafe = nullptr;
+#endif

 SignalSafe* InitSignalSafe() {
   gSignalSafe = Alloc<SignalSafe>();
+#ifndef OSH_IOS
   gHeap.RootGlobalVar(gSignalSafe);
+#endif

   RegisterSignalInterest(SIGINT);  // for KeyboardInterrupt checks
@@ -21,6 +30,17 @@
 }
 
 void RegisterSignalInterest(int sig_num) {
+#ifdef OSH_IOS
+  const osh_ios_config* config = osh_ios::CurrentConfig();
+  if (config != nullptr && config->set_signal_handler != nullptr) {
+    int result = config->set_signal_handler(sig_num, OurSignalHandler,
+                                            config->context);
+    if (result != 0) {
+      throw Alloc<OSError>(result);
+    }
+    return;
+  }
+#endif
   struct sigaction act = {};
   act.sa_handler = OurSignalHandler;
   if (sigaction(sig_num, &act, nullptr) != 0) {
@@ -36,6 +56,16 @@
   DCHECK(sig_num != SIGINT);
   DCHECK(sig_num != SIGWINCH);
 
+#ifdef OSH_IOS
+  const osh_ios_config* config = osh_ios::CurrentConfig();
+  if (config != nullptr && config->set_signal_handler != nullptr) {
+    int result = config->set_signal_handler(sig_num, handler, config->context);
+    if (result != 0) {
+      throw Alloc<OSError>(result);
+    }
+    return;
+  }
+#endif
   struct sigaction act = {};
   act.sa_handler = handler;
   if (sigaction(sig_num, &act, nullptr) != 0) {

--- a/mycpp/gc_mylib.cc
+++ b/mycpp/gc_mylib.cc
@@ -30,8 +30,13 @@
 }
 
 void print_stderr(BigStr* s) {
+#ifdef OSH_IOS
+  Stderr()->write(s);
+  Stderr()->write(StrFromC("\n"));
+#else
   fputs(s->data_, stderr);  // prints until first NUL
   fputc('\n', stderr);
+#endif
 }
 
 #if 0
@@ -106,7 +111,11 @@
   }
 }

+#ifdef OSH_IOS
+thread_local LineReader* gStdin = nullptr;
+#else
 LineReader* gStdin;
+#endif

 LineReader* open(BigStr* path) {
   // TODO: Don't use C I/O; use POSIX I/O!
@@ -190,6 +199,75 @@
+#ifdef OSH_IOS
+thread_local Writer* gStdout = nullptr;
+thread_local Writer* gStderr = nullptr;
+#else
 Writer* gStdout;
 Writer* gStderr;
+#endif
 
+#ifdef OSH_IOS
+class OshIosWriter : public Writer {
+ public:
+  void write(BigStr* s) override {
+    CHECK(target_ != nullptr);
+    target_->write(s);
+  }
+  void flush() override {
+    CHECK(target_ != nullptr);
+    target_->flush();
+  }
+  void close() override {
+  }
+  bool isatty() override {
+    return target_ != nullptr && target_->isatty();
+  }
+  Writer* SetTarget(Writer* target) {
+    Writer* old = target_;
+    target_ = target;
+    return old;
+  }
+  Writer* target() {
+    return target_;
+  }
+  static constexpr ObjHeader obj_header() {
+    return ObjHeader::ClassFixed(field_mask(), sizeof(OshIosWriter));
+  }
+  static constexpr uint32_t field_mask() {
+    return Writer::field_mask() | maskbit(offsetof(OshIosWriter, target_));
+  }
+
+ private:
+  Writer* target_ = nullptr;
+};
+
+thread_local OshIosWriter* gOshIosStdout = nullptr;
+thread_local OshIosWriter* gOshIosStderr = nullptr;
+
+void InitOshIosWriters() {
+  gOshIosStdout = Alloc<OshIosWriter>();
+  gOshIosStderr = Alloc<OshIosWriter>();
+  gStdout = gOshIosStdout;
+  gStderr = gOshIosStderr;
+  gHeap.RootGlobalVar(gOshIosStdout);
+  gHeap.RootGlobalVar(gOshIosStderr);
+}
+
+Writer* SetOshIosStdoutTarget(Writer* target) {
+  return gOshIosStdout->SetTarget(target);
+}
+
+Writer* SetOshIosStderrTarget(Writer* target) {
+  return gOshIosStderr->SetTarget(target);
+}
+
+Writer* OshIosStdoutTarget() {
+  return gOshIosStdout->target();
+}
+
+Writer* OshIosStderrTarget() {
+  return gOshIosStderr->target();
+}
+#endif
+
 //
 // CFileWriter
 //

--- a/mycpp/gc_mylib.h
+++ b/mycpp/gc_mylib.h
@@ -230,7 +230,11 @@
   DISALLOW_COPY_AND_ASSIGN(BufLineReader)
 };

+#ifdef OSH_IOS
+extern thread_local LineReader* gStdin;
+#else
 extern LineReader* gStdin;
+#endif

 inline LineReader* Stdin() {
   if (gStdin == nullptr) {
@@ -327,7 +331,11 @@
   bool is_valid_ = true;  // It becomes invalid after getvalue() is called
 };

+#ifdef OSH_IOS
+extern thread_local Writer* gStdout;
+#else
 extern Writer* gStdout;
+#endif

 inline Writer* Stdout() {
   if (gStdout == nullptr) {
@@ -337,7 +345,11 @@
   return gStdout;
 }

+#ifdef OSH_IOS
+extern thread_local Writer* gStderr;
+#else
 extern Writer* gStderr;
+#endif

 inline Writer* Stderr() {
   if (gStderr == nullptr) {
@@ -346,6 +358,14 @@
   }
   return gStderr;
 }
+
+#ifdef OSH_IOS
+void InitOshIosWriters();
+Writer* SetOshIosStdoutTarget(Writer* target);
+Writer* SetOshIosStderrTarget(Writer* target);
+Writer* OshIosStdoutTarget();
+Writer* OshIosStderrTarget();
+#endif
 
 class UniqueObjects {
   // Can't be expressed in typed Python because we don't have uint64_t for

--- a/mycpp/gc_alloc.h
+++ b/mycpp/gc_alloc.h
@@ -17,7 +17,11 @@
 extern BumpLeakHeap gHeap;
 #elif defined(MARK_SWEEP)
   #include "mycpp/mark_sweep_heap.h"
+#ifdef OSH_IOS
+extern thread_local MarkSweepHeap gHeap;
+#else
 extern MarkSweepHeap gHeap;
+#endif
 #endif

 // mycpp generates code that keeps track of the root set

--- a/mycpp/mark_sweep_heap.cc
+++ b/mycpp/mark_sweep_heap.cc
@@ -452,6 +452,10 @@
   #endif
 }

+#ifdef OSH_IOS
+thread_local MarkSweepHeap gHeap;
+#else
 MarkSweepHeap gHeap;
+#endif

 #endif  // MARK_SWEEP

--- a/mycpp/gc_iolib.h
+++ b/mycpp/gc_iolib.h
@@ -161,7 +161,11 @@
   int num_dropped_;
 };

+#ifdef OSH_IOS
+extern thread_local SignalSafe* gSignalSafe;
+#else
 extern SignalSafe* gSignalSafe;
+#endif

 // Allocate global and return it.
 SignalSafe* InitSignalSafe();

--- a/cpp/frontend_pyreadline.cc
+++ b/cpp/frontend_pyreadline.cc
@@ -18,7 +18,11 @@

 namespace py_readline {

+#ifdef OSH_IOS
+static thread_local Readline* gReadline = nullptr;
+#else
 static Readline* gReadline = nullptr;
+#endif

 // Assuming readline 4.0+
 #if HAVE_READLINE

--- a/cpp/libc.cc
+++ b/cpp/libc.cc
@@ -254,10 +254,14 @@

 namespace pylocale {
 BigStr* setlocale(int category, BigStr* locale) {
+#ifdef OSH_IOS
+  return StrFromC("");
+#else
   char* locale_name = ::setlocale(category, locale->data_);
   if (locale_name == nullptr) {
     throw Alloc<Error>();
   }
   return StrFromC(locale_name);
+#endif
 }
 BigStr* nl_langinfo(int item) {

--- a/mycpp/mark_sweep_heap.h
+++ b/mycpp/mark_sweep_heap.h
@@ -246,7 +246,10 @@
 class MarkSweepHeap {
  public:
   // reserve 32 frames to start
   MarkSweepHeap() {
   }
+#ifdef OSH_IOS
+  ~MarkSweepHeap() { FreeEverything(); }
+#endif

   void Init();  // use default threshold

--- a/cpp/stdlib.cc
+++ b/cpp/stdlib.cc
@@ -215,10 +215,18 @@
 time_t localtime(time_t ts) {
+#ifdef OSH_IOS
+  tm local_time;
+  tm* loc_time = ::localtime_r(&ts, &local_time);
+  if (loc_time == nullptr) {
+    throw Alloc<IOError>(errno);
+  }
+#else
   // localtime returns a pointer to a static buffer
   tm* loc_time = ::localtime(&ts);
+#endif

   time_t result = mktime(loc_time);
   if (result < 0) {
     throw Alloc<IOError>(errno);
   }
   return result;
 }
@@ -226,7 +234,15 @@
 BigStr* strftime(BigStr* s, time_t ts) {
+#ifdef OSH_IOS
+  tm local_time;
+  tm* loc_time = ::localtime_r(&ts, &local_time);
+  if (loc_time == nullptr) {
+    throw Alloc<IOError>(errno);
+  }
+#else
   tm* loc_time = ::localtime(&ts);
+#endif

   const int max_len = 1024;
   BigStr* result = OverAllocatedStr(max_len);
   int n = strftime(result->data(), max_len, s->data_, loc_time);
   if (n == 0) {
